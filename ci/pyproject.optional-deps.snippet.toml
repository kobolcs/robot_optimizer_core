# Merge into pyproject.toml if you want optional API support.

[project.optional-dependencies]
api = [
  "fastapi>=0.115",
  "uvicorn>=0.30",
]

test = [
  "pytest>=8.0",
  "pytest-cov>=5.0",
  "httpx>=0.27",
]

lint = [
  "ruff>=0.6",
  "pyright>=1.1",
]

docs = [
  "mkdocs>=1.6",
  "mkdocs-material>=9.5",
]
